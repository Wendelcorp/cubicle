<h2>home page</h2>

<% @spaces.each do |space| %>
  <div class = 'space info'>
    <%= space.name %>
    <%= link_to "spaces show page", space_path(space) %>
  </div>
  
<% end %>



<%= link_to "add a new space", new_space_path %>